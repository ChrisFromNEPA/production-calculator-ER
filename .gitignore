node_modules/
dist/
coverage/
playwright-report/
test-results/
*.log
.DS_Store
.vscode/
.idea/

# Local migration/build scratch
.tmp/
.tmp-*

# Generated runtime artifacts are rebuilt in CI when their source is changed.
# Keep committed generated files in the initial parity baseline only where the
# existing offline shell requires them.
.worktrees/
